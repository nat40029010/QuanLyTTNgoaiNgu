@model QuanLyTTNgoaiNgu.Models.KHOAHOC
@{
    Layout = "_Layout";
}
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <title>Sửa Khóa học</title>
    <style>
        body {
            margin: 0;
            background: linear-gradient(135deg, #4da0b0, #d39d38);
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: Arial, sans-serif;
        }

        .form-box {
            background: #fff;
            border-radius: 10px;
            padding: 40px;
            width: 450px;
            box-shadow: 0 8px 16px rgba(0,0,0,0.2);
        }

        h2 {
            text-align: center;
            margin-bottom: 25px;
            color: #333;
        }

        input, textarea {
            width: 100%;
            padding: 14px;
            margin-bottom: 15px;
            border-radius: 8px;
            border: 1px solid #ccc;
            font-size: 16px;
        }

        .btn {
            width: 100%;
            background: #ffc107;
            color: white;
            padding: 14px;
            font-weight: bold;
            border: none;
            border-radius: 8px;
            cursor: pointer;
        }

            .btn:hover {
                background: #e0a800;
            }
    </style>
</head>
<body>
    <div class="form-box">
        <h2>Sửa khóa học</h2>
        <form asp-action="Edit" method="post">
            <input asp-for="TenKhoaHoc" placeholder="Tên khóa học" />
            <span asp-validation-for="TenKhoaHoc" class="text-danger"></span>

            <textarea asp-for="MoTa" placeholder="Mô tả"></textarea>
            <span asp-validation-for="MoTa" class="text-danger"></span>

            <input asp-for="MucHocPhi" placeholder="Học phí" type="number" />
            <span asp-validation-for="MucHocPhi" class="text-danger"></span>

            <input asp-for="MaKhoaHoc" type="hidden" />
            <button type="submit" class="btn">Cập nhật</button>
        </form>
    </div>
</body>
</html>

